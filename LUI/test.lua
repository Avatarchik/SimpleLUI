import ('SimpleLUI', 'SimpleLUI.API.Core')
import ('SimpleLUI', 'SimpleLUI.API.Core.Math')

--local vector = SLUIVector2()
--vector.x = 1
--vector.y = 3
--core:Test1(vector)

local obj1 = core:Create()
local obj2 = core:Create()

-- obj1.RectTransform:SetParent(obj2.RectTransform)